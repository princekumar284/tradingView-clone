server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/trading
    username: dev
    password: dev
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  redis:
    host: localhost
    port: 6379

app:
  jwt:
    secret: very-very-secret-change-me-please
    expiration-ms: 3600000
    refresh-expiration-ms: 604800000
